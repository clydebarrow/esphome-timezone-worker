name = "esphome-timezone"
main = "src/index.ts"
compatibility_date = "2026-09-01"

# Uncomment and set to serve from your own domain instead of *.workers.dev
# routes = [{ pattern = "timezone.example.com", custom_domain = true }]
